/**
 * @deprecated Ne pas importer depuis la racine du clone.
 * Package publié : `main` / `exports` pointent vers `dist/` (build tsup).
 * Exemple : `import { Button } from '@equitech-dev/ui-library/Button'`
 */
export * from './dist/index.js';
